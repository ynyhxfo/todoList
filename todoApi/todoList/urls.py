# this file maybe not useful -- Jasper

# from .views import  createView

# from django.conf.urls import url

# urlpattern = [
#     url(r'^todoItem/$', createView.as_view, name='create'),
#     # url(r'^todoItem/(?P<pk>[0-9]+)/$',todoView.as_view(),name = 'todoDetail')
# ]